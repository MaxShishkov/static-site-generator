# Description

A tool that can generate static websites. I takes raw content files like Markdown and images and turns them
into a static website.

The result looks [like this](https://maxshishkov.github.io/static-site-generator/)
